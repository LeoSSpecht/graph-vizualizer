import { GraphTraversal } from "../services/GraphTraversal";

type TraversalStateProps = {
  graphTraversal: GraphTraversal;
};

export function TraversalState({ graphTraversal }: TraversalStateProps) {
  // Min Heap

  // Backtrack

  // Visited
  return;
}
